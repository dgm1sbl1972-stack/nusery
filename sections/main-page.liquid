<section class="section-pad">
  <div class="page-width page-width--narrow">
    <h1 class="section-title section-title--left">{{ page.title }}</h1>
    <div class="rte mt-40">{{ page.content }}</div>
  </div>
</section>

{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
